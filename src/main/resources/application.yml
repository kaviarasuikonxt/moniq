server:
  port: 8888

spring:
  application:
    name: moniq-api

  datasource:
    url: jdbc:postgresql://localhost:5432/moniq
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: moniq-secret-key-change-later
  expiration: 86400000

# ============ JWT ============
app:
  jwt:
    issuer: moniq
    exp-minutes: 60
    secret: ${JWT_SECRET}
    ttl: 3600
  # Where to redirect after OAuth2 success with token
  auth:
    frontend-callback: ${FRONTEND_URL}/auth/callback
    google:
      enabled: true

  # ============ OAuth2 Google ============
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: openid,profile,email
